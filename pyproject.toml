[project]
name = "fastapi-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi~=0.124.0",
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "uvicorn~=0.38.0",
]
